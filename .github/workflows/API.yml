name: API-Build

on:
  repository_dispatch:
    types: api-build

jobs:
  run-unit-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4.1.0
      - name: Form API
        run: |
          username=$(lscpu)
          echo "The current user is: $username"
  
