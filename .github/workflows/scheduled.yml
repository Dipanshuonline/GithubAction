name: scheduled

on:
  push:
  schedule:
  #- cron: "0 */8 * * *"
  - cron: "*/2 * * * *"
  
jobs:
  run-unit-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.0
      - name: run Selenium
        run: mvn test
      
